pipeline {
    agent any  // Définit l'agent sur "none" pour indiquer que le pipeline s'exécute sur le nœud maître

    stages {
     stage("test"){
             steps{
                 echo "hello world"
             }
         }
    }
}
